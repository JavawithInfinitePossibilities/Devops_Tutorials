1.  Introduction
2.  Docker Architecture and Workflow
3.  Docker in action
    1.  Create a new AWS Instance.
    2.  Install Docker
        #yum install docker -y
        #docker --version
        Check the status
        #docker info
        To start docker service
        #service docker start
        To start a docker Image
        #docker run hello-world
        #docker run -it ubuntu bash
        #docker pull mysql:5.7

4.  Docker Run
    #docker run --help
    #docker run -i -d -t -p 80:80 nginx
    #docker ps
    #docker stop <pid from ps commend>
    #docker ps -a
    #docker rm <container id from ps -a commend>
    #docker rmi <container id from image commend>
    #docker restart <container id from ps commend>
    #docker pause <container id from ps commend>
    #docker run -idtp 80:80 --name=mycontainerfortest nginx

5.  Docker Clean Up
6.  Get Comfortable with Docker Run
7.  Docker Commit
    #docker commit <container id from ps commend> new-image-name

8.  Docker Layers and Overlay Storage
9.  Launch MySql Container
    #docker run -d -p 6666:3306 --name=docker-mysql --env="MYSQL_ROOT_PASSWORD=test1234" --env="MYSQL_DATABASE=emp" mysql
    #docker exec -it docker-mysql bash
    $/mysql -uroot -p 
    mysql> show databases;
    mysql> show tables; 

10. Volumes and Bind Mounts
11. Use Volumes
    #docker volumn ls
    #docker volumn create <new_volumn_name>
    #docker run -dit --mount source=<new_volumn_name>,destination=/tmp nginx
    #ls /var/lib/docker/volumes/<new_volumn_name>/_data
    #docker ps 
    #docker exec -it <ps container id> bash

12. Use Bind Mounts
    #mkdir mydata
    #docker run -dit -v /root/mydata:/tmp ubuntu
    #docker ps
    #docker exec -it <ps container id> bash

13. Docker networking in action
    #docker network ls
    #docker network inspect bridge
    #docker network create demonw --subnet=<get the value>
    #docker network ls

14. Change a containers network
15. Windows - Install Docker
16. Dockerfile Introduction
17. First Dockerfile
18. Beauty of Docker Layers
19. Docker